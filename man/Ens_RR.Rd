% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EnsModel.R
\name{Ens_RR}
\alias{Ens_RR}
\title{Computes the probability of rejecting the set when (Rejection Rate) for the
 Ensemble Model}
\usage{
Ens_RR(
  criterion = 0.25,
  mu = 1,
  sigmaT = 1,
  rho_0 = 0,
  rho_1 = 0,
  nSize = 4,
  method.Ens = "base",
  verbose = FALSE,
  ...
)
}
\arguments{
\item{criterion}{: Value of the criterion for selection}

\item{mu}{: Mean of the target marginal distributions}

\item{sigmaT}{: Standard deviation of the target marginal distributions}

\item{rho_0}{: Value of the common correlation among non-target stimuli}

\item{rho_1}{: Value of the common correlation among non-target stimuli and the target}

\item{nSize}{: Number of simultaneously presented stimuli}

\item{method.Ens}{: Whether to integrate using base-R integrate (if set to "base" default. More precise, slower) or
mvtnorm::pmvnorm (if set to "mvnorm". Less precise, faster)}

\item{verbose}{: Logical. If TRUE print the nested integral to be computed when method.Ens == 'base'}

\item{...}{: Captures unused parameter when called from generic function unforcedChoice_RRTA()}
}
\value{
numeric value of the Hit Rate
}
\description{
Computes the probability of rejecting the set when (Rejection Rate) for the
 Ensemble Model
}
\examples{
unforcedChoice:::Ens_RR(mu = 1, sigmaT = 1, nSize = 2, criterion = .25)
          unforcedChoice:::Ens_RR(mu = 1, sigmaT = 1, nSize = 2, criterion = .50)
          unforcedChoice:::Ens_RR(mu = 1, sigmaT = 1, nSize = 2, criterion = -Inf)

}
